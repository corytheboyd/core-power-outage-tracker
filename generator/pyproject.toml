[project]
name = "core-power-outage-map"
version = "0.1.0"
description = "Core Power Outage Map"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.1.0",
    "numpy>=2.4.0",
    "pandas>=2.3.3",
]
