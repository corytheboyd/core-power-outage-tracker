import type { FunctionComponent } from "react";
import Typography from "@mui/material/Typography";

export const NeedGeolocationPage: FunctionComponent = () => {
  return (
    <div>
      <Typography>Need Geolocation</Typography>
    </div>
  );
};
